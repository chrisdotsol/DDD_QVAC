User query:

{{query}}

Current local DDD snapshot:

{{snapshot_json}}

Retrieved local context:

{{retrieved_chunks}}

Answer requirements:

- Use only the snapshot and retrieved context above.
- Include Answer, Data Used, Sources Used, and Missing Data sections.
- If a field is absent, write "not available in the current local dataset".
- Keep the response neutral and factual.
